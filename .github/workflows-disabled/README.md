# Disabled GitHub Actions

This folder contains workflows that were previously under `.github/workflows/`.

They were moved here to prevent GitHub Actions from running while the project is being validated locally.

## Re-enable

Move the `.yml` files back into `.github/workflows/`.
